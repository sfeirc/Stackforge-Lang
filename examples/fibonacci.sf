// Recursive fibonacci: fib(0)..fib(9), one per line.
fn fib(n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

for (let i = 0; i < 10; i = i + 1) {
    print fib(i);
}
